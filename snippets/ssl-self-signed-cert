function sslselfsignedcert () {
    if [[ $# -ne 2 ]]; then
        echo "Usage: sslselfsignedcert KEY CERT"
    else
        openssl req -new -newkey rsa:1024 -days 365 -nodes -x509 -keyout $1 -out $2
    fi
}
sslselfsignedcert 
