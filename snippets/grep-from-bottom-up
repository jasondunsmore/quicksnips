# Searches a file from bottom up.
# Usage: grepfrombottomup REGEX FILE

function grepfrombottomup () {
    if [[ $# -ne 2 ]]; then
        echo "Usage: grepfrombottomup REGEX FILE"
    else
        tac "$2"|egrep --color "$1"
    fi
}
grepfrombottomup
