# Prints the amount of httpd bandwidth used for each day of the month.

for day in `seq -w 01 31`; do echo -n "`date +%B` $day: "; egrep "$day/`date +%b`" /var/log/httpd/access_log* | awk '{ SUM += $10 } END {print SUM/1024/1024, "M"}'; done
