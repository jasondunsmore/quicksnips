names='user1 user2'; if [ ! -e /usr/bin/mkpasswd ]; then yum install expect || up2date -i expect; fi && for x in $names; do pas=`mkpasswd`; (echo "$x => $pas" >> p); (echo $pas|passwd --stdin $x); done && cat p && rm -f p
